function checkStats() {
  const username = document.getElementById('username').value;
  const resultDiv = document.getElementById('result');

  resultDiv.innerText = `Checking stats for ${username}...`;

  // Placeholder — later we'll add API calls here
}
